---
profile: production

exclude_paths:
  - .github/
  - sandbox.md

skip_list:
  # Package versions are pinned through kubernetes_patch_version, which is the
  # whole point of the role.
  - package-latest
