---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true

- name: Restart kubelet
  ansible.builtin.systemd_service:
    name: kubelet
    state: restarted
    daemon_reload: true

- name: Reload ufw
  community.general.ufw:
    state: reloaded
