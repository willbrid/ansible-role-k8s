# Vagrant machine state, specific to the host running the sandbox.
.vagrant/

# Guest Additions image, downloaded on demand (see README.md).
*.iso

# Inventory regenerated by bin/gen-inventory.sh with the real SSH key path.
inventory/hosts.ini

*.log
*.retry
